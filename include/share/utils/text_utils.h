#include <string>
using namespace std;

#ifndef TEXT_UTILS
    #define TEXT_UTILS

    class TextUtils {
        public:
            static string to_upper(string);
            static string to_lower(string);
    };

#endif